<% content_for :title, "Editing session" %>

<div class="md:w-2/3 w-full">
  <h1>Editing session</h1>

  <%= render "form", session: @session %>

  <%= link_to "Show this session", @session, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <%= link_to "Back to sessions", sessions_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
