<% if page.current? %>
  <span class="bg-indigo-400 text-white px-3 py-1 rounded"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "px-4 text-gray-800 text-sm" %>
<% end %>
