<div class="w-full">
  <% content_for :title, "Dashboard" %>
  <div class="container mx-auto px-4">
    <h1 class="text-2xl font-bold mb-4 sm:mb-0">Dashboard</h1>
    <br/>
    Great progress! You're making a positive impact:<br/><br/>
    <ul class="list-disc ml-4">
    <li><strong><%= pluralize(@clients_count, "life") %> touched</strong> through your coaching</li>
    <li><strong><%= pluralize(@hours_count, "hour") %></strong> dedicated to empowering others<br/></li>
    <li><strong><%= @coaching_sessions_count %></strong> transformative <strong><%= @coaching_sessions_count == 1 ? "session" : "sessions" %></strong> delivered<br/></li>
    <li><strong><%= @coaching_sessions_paid %>%</strong> of your valuable time <strong>compensated</strong></li>
    </ul>
    <br/>
    Keep up the fantastic work! Every session brings you closer to your goals.
  </div>
</div>
